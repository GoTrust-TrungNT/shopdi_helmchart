apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopdi-payment.fullname" . }}
  labels:
    {{- include "shopdi-payment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopdi-payment.selectorLabels" . | nindent 4 }}
