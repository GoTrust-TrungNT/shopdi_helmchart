apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shopdi_backend_system_test.fullname" . }}-test-connection"
  labels:
    {{- include "shopdi_backend_system_test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shopdi_backend_system_test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
