apiVersion: v1
kind: Service
metadata:
  name: {{ include "shopdi_backend_system_test.fullname" . }}
  labels:
    {{- include "shopdi_backend_system_test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shopdi_backend_system_test.selectorLabels" . | nindent 4 }}
